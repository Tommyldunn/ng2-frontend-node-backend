# Angular2 frontend with seperate backend

##### Setting it up
- clone repo
- cd backend `npm install` or `yarn install`
- cd frontend `npm install` or `yarn install` 

##### Starting
- `npm start` for both front and back
